export interface Company {
    id?: string; //MONGOID OBIEKTU
    _id?: string;
    name: string;
    address: string;
    postalcode: string;
    city: string;
    nip: string;
    category: string;
}
