export interface Rec {
    id?: string;//MONGOID OBIEKTU
    _id?: string; //MONGOID OBIEKTU
    recName: string;
    name: string; //prod name
    clS: string;
    con: string;
    clW: string;
    clF: string;
    cert: boolean;
    agTime: number;
    clX: string[];
    clN:string;
    comments: string;
}
